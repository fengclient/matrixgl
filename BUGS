KNOWN BUGS
- Runs on OSX, but doesn't support the mac version of xscreensaver and 
therefore can't be used as a screensaver.
- The --limit option doesn't work exactly as described. It only really
works on really fast setups where it would otherwise run at >300fps.
